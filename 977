class Solution:
    def sortedSquares(self, nums: List[int]) -> List[int]:
        square=[]
        for i in nums:
            a=i**2
            square.append(a)
        return sorted(square)
        
